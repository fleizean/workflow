/*
 * The History route - an empty stub (D-12). No data, no state, no store: the work-history screen is
 * ported in Phase 7, against the services and bridge the phases before it build.
 */

import type { ReactElement } from 'react';

export default function HistoryRoute(): ReactElement {
    return <h1>History</h1>;
}
