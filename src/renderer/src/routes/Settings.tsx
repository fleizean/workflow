/*
 * The Settings route - an empty stub (D-12). No data, no state, no store: the screen is ported in
 * Phase 7, against the services and bridge the phases before it build.
 */

import type { ReactElement } from 'react';

export default function SettingsRoute(): ReactElement {
    return <h1>Settings</h1>;
}
